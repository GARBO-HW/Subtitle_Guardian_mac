<UserControl x:Class="SubtitleGuardian.App.Views.SrtToTxtPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SubtitleGuardian.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header / Controls -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="SRT 檔案" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="SrtPathText" Grid.Column="1" Height="28" IsReadOnly="True" Margin="0,0,8,0" AllowDrop="True" PreviewDragOver="OnDragOver" Drop="OnSrtDrop"/>
            <Button x:Name="PickSrtButton" Content="選取檔案" Grid.Column="2" Width="100" Height="28" Click="OnPickSrt"/>
            <Button x:Name="ClearButton" Content="清除" Grid.Column="3" Width="80" Height="28" Margin="8,0,0,0" Click="OnClear"/>
        </Grid>

        <!-- Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Input -->
            <GroupBox Header="SRT 內容 (可直接貼上)" Grid.Column="0" Margin="0,0,5,0">
                <TextBox x:Name="SrtContentText" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" 
                         TextChanged="OnSrtContentChanged" FontFamily="Consolas" Padding="5"/>
            </GroupBox>

            <!-- Middle Actions -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="5,0">
                <Button Content="轉換 &gt;&gt;" Width="80" Height="30" Click="OnConvertClick"/>
            </StackPanel>

            <!-- Output -->
            <GroupBox Header="TXT 結果" Grid.Column="2" Margin="5,0,0,0">
                <TextBox x:Name="TxtContentText" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" 
                         IsReadOnly="True" FontFamily="Consolas" Padding="5"/>
            </GroupBox>
        </Grid>

        <!-- Footer Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="Gray"/>
            <Button Content="複製結果" Width="100" Height="28" Margin="0,0,10,0" Click="OnCopyResult"/>
            <Button Content="匯出 TXT" Width="100" Height="28" Click="OnExportTxt"/>
        </StackPanel>
    </Grid>
</UserControl>